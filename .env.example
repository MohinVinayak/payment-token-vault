# Encryption keys (generate your own, never commit real keys)
FERNET_KEY=your_url_safe_base64_encoded_32_byte_key
HMAC_SECRET=your_secure_random_hex_string

# JWT
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRY_MINUTES=30

# Token settings
TOKEN_TTL_HOURS=24
